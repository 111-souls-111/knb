meta:
  environment: python
  toolchain: pip

build:
  requirements: requirements.txt

run:
  command: gunicorn --worker-class eventlet --bind 0.0.0.0:8080 app_online:app
  persistenceMount: /data
  containerPort: 8080
